---
description: Show the callmem startup briefing with recent context and open tasks
---

Run the bash command `callmem briefing` in the current project and present its output to me verbatim (preserve the box-drawing and formatting). Do not read `SESSION_SUMMARY.md` — that file is a stale snapshot; the CLI is the live source. After presenting it, ask what I'd like to work on.
