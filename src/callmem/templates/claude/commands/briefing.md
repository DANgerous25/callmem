---
description: Show the callmem startup briefing with recent context and open tasks
---

Run the bash command `callmem briefing` in the current project. Then reproduce its ENTIRE stdout inside a single fenced code block (```text ... ```), character-for-character — every line, all box-drawing, all entity IDs. Do NOT summarize, shorten, or paraphrase it; do NOT claim it is shown unless the fenced block is actually present in your reply. Do not read `SESSION_SUMMARY.md` — that file is a stale snapshot; the CLI is the live source. After the fenced block, ask what I'd like to work on.
